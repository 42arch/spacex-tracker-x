import React from 'react'

const LaunchRecordSection = () => {
	return (
		<section id='launchs' className='w-full h-[640px] bg-slate-600'>
			LaunchRecordSection
		</section>
	)
}

export default LaunchRecordSection